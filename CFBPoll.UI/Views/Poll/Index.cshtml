@model CFBPoll.UI.ViewModels.Poll.CFBPollViewModel
@{
    ViewData["Title"] = "Poll";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var request = Context.Request;
    var baseUrl = $"{request.Scheme}://{request.Host}{request.PathBase}";
}

<html>
    <head>
        <script src="@(baseUrl)/js/pollfunctions/pollfunctions.js"></script>
        <link rel="stylesheet" href="~/css/poll/poll.css"/>
    </head>
    <body>
        <div id="selectionsContainer">
            @await Html.PartialAsync("_SelectionsPartial", Model.Selections)
        </div>
        <br />
        <div id="rankingsContainer">
            @await Html.PartialAsync("_RankingsPartial", Model.Rankings)
        </div>
    </body>
</html>